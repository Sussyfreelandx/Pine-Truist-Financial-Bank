{
  "name": "@pine/web",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "NODE_OPTIONS=\"${NODE_OPTIONS:-} --max-http-header-size=${MAX_HTTP_HEADER_SIZE_BYTES:-65536}\" vite preview --port ${PORT:-3000} --host",
    "start": "NODE_OPTIONS=\"${NODE_OPTIONS:-} --max-http-header-size=${MAX_HTTP_HEADER_SIZE_BYTES:-65536}\" vite preview --port ${PORT:-3000} --host"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "react-router-dom": "^6.27.0",
    "socket.io-client": "^4.8.0",
    "zustand": "^4.5.5"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.47",
    "tailwindcss": "^3.4.14",
    "vite": "^5.4.10"
  }
}
